import { CageGridDirective } from './cage-grid.directive';

describe('CageGridDirective', () => {
  it('should create an instance', () => {
    const directive = new CageGridDirective();
    expect(directive).toBeTruthy();
  });
});
