import { MapExternalPipe } from './map-external.pipe';

describe('MapExternalPipe', () => {
	it('create an instance', () => {
		const pipe = new MapExternalPipe();
		expect(pipe).toBeTruthy();
	});
});
